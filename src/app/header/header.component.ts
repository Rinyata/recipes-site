import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  templateUrl: './header.component.html',
  styleUrls: ['./header.component.css']
})
export class HeaderComponent {

  // searchBar!: HTMLElement;

  // // Sayfa yüklendiğinde searchBar değişkenini alacak olan fonksiyon
  // window.addEventListener('DOMContentLoaded', () => {
  //   searchBar = document.getElementById('searchBar')!;
  // });
  
  // // Fare search barın üzerine geldiğinde
  // searchBar.addEventListener('mouseenter', () => {
  //   searchBar.style.width = '200px'; // Genişliği 200 piksel yap
  // });
  
  // // Fare search barın üzerinden çekildiğinde
  // searchBar.addEventListener('mouseleave', () => {
  //   searchBar.style.width = '150px'; // Genişliği başlangıç genişliğine (150px) döndür
  // });
  

  
}
